<UserControl x:Class="InventoryApp.Views.SalesListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:InventoryApp.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <UserControl.DataContext>
        <vm:SalesListViewModel />
    </UserControl.DataContext>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <ItemsControl ItemsSource="{Binding Sales}" Margin="10">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="1" BorderBrush="Gray" Padding="10" Margin="5">
                        <StackPanel>
                            <TextBlock FontSize="16" FontWeight="Bold">
                                <Run Text="Поръчка №: " />
                                <Run Text="{Binding Id}" />
                            </TextBlock>
                            
                            <TextBlock FontStyle="Italic">
                                <Run Text="Дата на поръчка: " />
                                <Run Text="{Binding Date, StringFormat={}{0:dd.MM.yyyy HH:mm}}" />
                            </TextBlock>

                            <ItemsControl ItemsSource="{Binding ProductSales}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="5">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <TextBlock Text="{Binding Product.Name}" />
                                            <TextBlock Grid.Column="1" Margin="30,0"
                                                       Text="{Binding Quantity}" />
                                            <TextBlock Grid.Column="2"
                                                       Text="{Binding Product.Price, StringFormat=F2}" />
                                           
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>