<UserControl x:Class="InventoryApp.Views.CartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:InventoryApp.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">


    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0 0 0 10">
            <Button Content="Изчисти количката" Command="{Binding ClearCartCommand}" Margin="5"/>
            <Button Content="Поръчай" Command="{Binding CheckoutCommand}" Margin="5"/>

        </StackPanel>

        <ListView ItemsSource="{Binding CartItems}" Margin="0 0 0 10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Продукт" DisplayMemberBinding="{Binding Product.Name}" />
                    <GridViewColumn Header="Цена" DisplayMemberBinding="{Binding Product.Price}" />
                    <GridViewColumn Header="Количество">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="-" Width="20" Command="{Binding DataContext.DecreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                                    <TextBlock Text="{Binding Quantity}" Width="30" TextAlignment="Center"/>
                                    <Button Content="+" Width="20" Command="{Binding DataContext.IncreaseQuantityCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Header="Общо" DisplayMemberBinding="{Binding TotalPrice}" />
                    <GridViewColumn Header="Действие">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Премахни" Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

     
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>  
              
                <TextBlock  Grid.Row="0"  Text="Общо количество:" FontWeight="Bold" Margin="0 0 10 0"/>
                <TextBlock  Grid.Row="1"  Text="{Binding TotalQuantity}" Margin="5"/>
            <TextBlock Grid.Row="2" Text="Обща сума:" FontWeight="Bold"  Margin="0 0 10 0"/>
            <TextBlock Grid.Row="3" Text="{Binding TotalPrice}"  Margin="5" />
                   
            </Grid>
           
    
    </DockPanel>
</UserControl>
